# Copy to .env.local for local development.
# Base URL of the Odoo backend. Left empty to use the Vite dev proxy
# (recommended) or set explicitly for production builds.
VITE_ODOO_URL=http://localhost:8069
