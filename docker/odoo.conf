[options]
addons_path = /mnt/extra-addons,/usr/lib/python3/dist-packages/odoo/addons
data_dir = /var/lib/odoo
; IMPORTANT: change this before any production deployment - Odoo config
; files are plain INI, not shell-interpolated, so this must be edited
; directly (or overridden via the --config flag with a generated file).
admin_passwd =changeme_in_production
db_host = db
db_port = 5432
db_user = odoo
db_password =odoo
db_name = sanad_db
list_db = True
proxy_mode = True
workers = 0
